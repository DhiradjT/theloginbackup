{% extends 'base.html.twig' %}

{% block body %}

    <h1>Add lesson</h1>

    {{ form_start(form) }}
    {{ form_row(form.location) }}
    {{ form_row(form.max_persons) }}
        {{ form_row(form.date) }}
        {{ form_row(form.time) }}
    {{ form_row(form.title) }}
    <button type="submit" class="btn btn-primary">Create</button>
    {{ form_end(form) }}


{% endblock %}