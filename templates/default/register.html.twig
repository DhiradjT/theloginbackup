{% extends 'base.html.twig' %}

{% block body %}

    <h1>Registreren</h1>

    <div>
        {{ form_start(form) }}
        <button type="submit">Submit</button>
        {{ form_end(form) }}
    </div>

{% endblock %}