{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <h1>Registreren</h1>

            <div>
                {{ form_start(form) }}
                <button type="submit" class="btn btn-primary">Save Changes</button>
                {{ form_end(form) }}
            </div>

        </div>
    </div>


{% endblock %}